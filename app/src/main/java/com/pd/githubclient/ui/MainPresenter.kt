package com.pd.githubclient.ui

import com.pd.githubclient.domain.Model

class MainPresenter(val view: MainView) {
    val model = Model()

    fun onLoadRepositoryList() {   //метод загрузки списка репозиториев по событию (клику) из вью

    }
}